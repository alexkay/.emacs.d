`ln -s` or clone the repo to `$HOME/.emacs.d`

`M-x package-list-packages` and install:

* go-mode
* haskell-mode
* less-css-mode
* markdown-mode
* pandoc-mode
* php-mode
* puppet-mode
* rust-mode
* solarized-dark
